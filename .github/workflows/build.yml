name: Build Nuitka Executables

on:
  push:
    branches: [main, dev]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Install ccache
        run: sudo apt-get install -y ccache

      - name: Install project dependencies
        run: uv sync --frozen --no-install-project

      - name: Cache Nuitka compilation files
        uses: actions/cache@v4
        with:
          path: |
            .nuitka-cache
            dist/main.build
            dist/main.onefile-build
          key: nuitka-linux-${{ hashFiles('uv.lock', 'pyproject.toml', 'main.py') }}
          restore-keys: |
            nuitka-linux-

      - name: Build executable with Nuitka (onefile)
        env:
          NUITKA_CACHE_DIR: ${{ github.workspace }}/.nuitka-cache
        run: |
          uv run --with-editable . --group build \
            python -m nuitka \
              --onefile \
              --standalone \
              --output-dir=dist \
              --output-filename=tiktok-sequencia \
              --assume-yes-for-downloads \
              main.py

      - name: Verify and inspect executable
        run: |
          if [ ! -f "dist/tiktok-sequencia" ]; then
            echo "::error::Executable not found after build"
            ls -la dist/ || true
            exit 1
          fi
          chmod +x dist/tiktok-sequencia
          du -h dist/tiktok-sequencia

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tiktok-sequencia-linux${{ github.ref_name == 'dev' && '-dev' || '' }}
          path: dist/tiktok-sequencia
          if-no-files-found: error
          compression-level: 6
          retention-days: 14

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Install project dependencies
        run: uv sync --frozen --no-install-project

      - name: Cache Nuitka compilation files
        uses: actions/cache@v4
        with:
          path: |
            .nuitka-cache
            dist/main.build
            dist/main.onefile-build
          key: nuitka-windows-${{ hashFiles('uv.lock', 'pyproject.toml', 'main.py') }}
          restore-keys: |
            nuitka-windows-

      - name: Build executable with Nuitka (onefile)
        shell: pwsh
        env:
          NUITKA_CACHE_DIR: ${{ github.workspace }}/.nuitka-cache
        run: |
          uv run --with-editable . --group build `
            python -m nuitka `
              --onefile `
              --standalone `
              --output-dir=dist `
              --output-filename=tiktok-sequencia `
              --assume-yes-for-downloads `
              --windows-console-mode=disable `
              main.py

      - name: Verify and inspect executable
        shell: pwsh
        run: |
          $exe = "dist\tiktok-sequencia.exe"
          if (-not (Test-Path $exe)) {
            Write-Error "Executable not found after build"
            Get-ChildItem dist\ -ErrorAction SilentlyContinue
            exit 1
          }
          $size = (Get-Item $exe).Length / 1MB
          Write-Output ("{0:N2} MB" -f $size)

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tiktok-sequencia-windows${{ github.ref_name == 'dev' && '-dev' || '' }}
          path: dist/tiktok-sequencia.exe
          if-no-files-found: error
          compression-level: 6
          retention-days: 30
